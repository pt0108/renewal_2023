import { Container } from "react-bootstrap";

function Works() {
    return (
        <Container id="Works" style={{ minHeight: "100vh"}}>
            Works
        </Container>
    );
}

export default Works;